# 2026-07-03 Chrome E2E Notes

## Scope

Chrome-native validation ran against local PM2 production services:

- Main: `http://127.0.0.1:3000`
- Admin: `http://127.0.0.1:3001`
- Evidence folder: `docs/product-audits/2026-07-03-chrome-e2e/screenshots/`

The Chrome extension backend connected after the Chrome plugin cache updated to `26.623.101652`. The audit used Chrome screenshots, read-only page evaluation, locators, and console logs. The Chrome `domSnapshot()` API returned an extension-side compatibility error, so it was not used as evidence.

## Step Results

1. Public discovery: `01-home.png`
   Health: pass. Home/explore loaded with navigation, search/filter controls, character cards, logged-in header, and no console errors.
2. Auth redirect and anonymous state: `02-login.png`, `03-signup.png`, `04-logged-out-home.png`
   Health: pass. Existing logged-in user redirected away from auth pages; logout exposed Login and Join Free.
3. Signup and returning login: `05-login-form.png`, `06-signup-form.png`, `07-after-signup.png`, `08-after-login.png`
   Health: pass. New test user `chrome-e2e-1783069208828@test.local` signed up, logged out, logged back in, and returned to product shell without console errors.
4. Character detail: `09-character-detail.png`, `10-character-detail-loaded.png`
   Health: pass. Detail page loaded from initial loading state to Melissa Burke detail with Chat, Like, and Report.
5. Chat: `11-chat-opened.png`, `12-chat-message-reply.png`, `chat-message-edited.png`
   Health: pass. Character detail created `/chat/sess_*`, user message sent, assistant reply returned, and send control recovered. Incremental edit check replaced `chrome chat edit original 1783072407618` with `chrome chat edit replacement 1783072407618`, regenerated the paired assistant reply, and Chrome console errors were `[]`.
6. Image generation: `13-generator.png`, `14-generator-after-job.png`, `generate-user-preset-ui.png`
   Health: pass. Generator showed balance, character controls, model/preset controls, Premium prompt lock, active jobs, and gallery. One image job completed, balance changed from 250 to 245, and generated media appeared. Incremental My Presets check saved `Chrome Preset 1783078667824`, applied it back to `Background=Studio`, deleted it through the UI, confirmed it disappeared from the page, and Chrome console errors were `[]`.
7. Upgrade and profile: `15-upgrade.png`, `16-after-demo-upgrade.png`, `17-profile.png`, `18-profile-actions.png`, `19-profile-resumed.png`, `profile-library-search.png`
   Health: pass. Demo checkout activated Premium monthly, added 1,500 dreamcoins, profile showed Premium monthly and generated media, referral invite produced copy action, renewal canceled and resumed. Incremental My AI search check matched `chrome profile search needle` as a single media card, showed the no-match state, then Clear search restored 14 cards; Chrome console errors were `[]`.
8. Community and feed: `20-community.png`, `21-feed.png`, `22-community-followed.png`, `23-feed-liked.png`
   Health: pass. Community showed dreamers/characters/collections; Follow changed to Following. Feed showed recommended dreams; Like changed to Liked.
9. Help Desk: `24-helpdesk.png`, `25-helpdesk-submitted.png`, `helpdesk-appeal-submitted.png`
   Health: pass. Required fields enabled Submit request; submission returned `SUP-Y8P39H5L37`. Incremental Appeal check submitted target `chrome-appeal-1783071479623`, returned `Appeal cmr4qpzbn000kc8l739czbeik submitted.`, DB confirmed `status=open`, and Chrome console errors were `[]`.
10. Character creation and My AI: `26-create.png`, `27-create-step1-filled.png`, `28-create-step-2.png`, `28-create-step-3.png`, `28-create-step-4.png`, `28-create-step-5.png`, `29-character-saved.png`, `30-my-ai.png`, `my-ai-created-character-edit.png`
    Health: pass. The multi-step builder reached Publish, saved private character `Chrome Muse 1783069864914`, and My AI listed it. Incremental Created management check created `Chrome Edit 1783077291838`, edited it in My AI to `Chrome Edit Updated 1783077291838`, updated the description inline, confirmed the card refreshed, and Chrome console errors were `[]`.
11. Admin login and dashboard: `31-admin-entry.png`, `32-admin-dashboard.png`
    Health: pass. Admin dev login accepted `admin/admin123`; dashboard loaded metrics, feature flags, and full admin navigation.
12. Admin operations: `33-admin-generation-jobs.png`, `34-admin-generation-config.png`, `35-admin-provider-health.png`, `36-admin-review-queue.png`, `37-admin-support.png`, `38-admin-users.png`, `39-admin-billing.png`
    Health: pass. Key admin routes loaded with admin shell and zero console errors.
13. Responsive navigation: `chrome-navigation-responsive-smoke.png`
    Health: pass. Chrome desktop responsive smoke at 1512px verified sidebar navigation and character cards visible, mobile bottom navigation hidden at the desktop breakpoint while its semantic links remain present, and console errors were `[]`. The 390px mobile-specific contract is covered by Playwright E2E: fixed bottom nav, Explore/Chat/Create/Generate links, active state, two-column cards, and no bottom-nav occlusion.
14. Explore ranking: `explore-ranking-menu-following.png`
    Health: pass. Chrome verified the Explore sort menu exposes `For You`, `Popular · Month`, `Newest`, and `Following`; selecting `Following` synced the URL to `?sort=following&gender=any&limit=4`, rendered the Following-specific empty state `No followed characters yet`, and console errors were `[]`.
15. Admin dead-letter discard: `admin-dead-letter-discard.png`
    Health: pass. Chrome opened `/admin/generation/dead-letter`, selected failed job `chrome-dl-discard-1783079586512-786244` via its accessible row checkbox, submitted batch `Discard` with typed confirmation, saw the row removed and selected count return to zero, and Chrome console errors were `[]`. DB verification before cleanup showed `GenerationJob.status=refunded`, exactly one refund ledger entry with `delta=9`, and one `ops.deadletter.discard` audit entry.
16. Admin support SLA: `admin-support-sla-overdue.png`
    Health: pass. Chrome opened `/admin/support`, filtered search `Chrome SLA mr4wf5v0`, `status=active`, `SLA=overdue`, and `category=generation`; the table showed only `SUP-SLA-mr4wf5v0`, hid fresh on-track sibling `SUP-SLF-mr4wf5v0`, displayed `已超时`, and Chrome console errors were `[]`. Fixture cleanup deleted both support requests and the temporary user.
17. Admin support SLA escalation: `admin-support-sla-escalate.png`
    Health: pass. Chrome opened `/admin/support`, filtered search `Chrome Escalate mr4wy2yo`, `status=active`, `SLA=overdue`, and `category=generation`; the table showed only `SUP-ESC-mr4wy2yo`, hid fresh on-track sibling `SUP-ESF-mr4wy2yo`, submitted Escalate with reason `Chrome escalated overdue support ticket`, then displayed priority `1`, escalation time, escalation reason, and assigned admin email while removing the Escalate action. DB verification before cleanup showed `support.request.escalate` audit, `slaEscalatedAt`, `slaEscalatedById`, and `slaEscalationReason`; Chrome console errors were `[]`. Fixture cleanup deleted both support requests, the temporary user, and the escalation audit row.
18. Profile media to Community collection: `community-collection-published.png`
    Health: pass. Chrome used the active local account on `/profile`, opened the media tab, found temporary media `chrome-collection-media-mr4xcollection`, created `Chrome Collection mr4xcollection` with `Publish to Community` checked, then opened `/community` and verified the public collection card showed `1 items · by Admin`. A targeted page check confirmed the collection preview image loaded (`naturalWidth=179`, `naturalHeight=225`). DB verification before cleanup showed collection `visibility=public`, item count 1, media `visibility=public_pack`, and one collection analytics event; fixture cleanup returned 0 remaining collection/media/event rows. Chrome console errors were `[]`.
19. Feed share/remix provenance: `feed-share-remix-focused.png`, `feed-remix-generate-selected.png`
    Health: pass. Chrome opened `/feed?item=character%3Achrome-feed-remix-1783084676386-character`, verified the focused shared card, `Showing shared dream.`, `Shared dream`, target character/creator links, Share status `Share link copied.`, and Remix navigation to `/generate?characterId=chrome-feed-remix-1783084676386-character&remixFeedItemId=character%3Achrome-feed-remix-1783084676386-character`. Generate selected `Chrome Feed Remix chrome-feed-remix-1783084676386` instead of the default first-page character, showed `Remix ready from Feed. Adjust details and generate.`, and queued a generation job. DB verification before cleanup showed `sourceType=feed_remix`, matching `characterId`, and `sourceMeta.feedItemId/sourceCharacterId/sourceCreatorId/sourceCharacterName`; cleanup returned 0 remaining fixture jobs/events/ledger/media/analytics/character/user rows. Chrome console errors and warnings were `[]`.
20. Feed fallback image and LCP pass: `feed-lcp-fallback-eager.png`
    Health: pass. Chrome opened production-bundle `/feed` after the Next 16 image-loading migration and fallback-image fix. Feed rendered 16 cards with 13 unique large image URLs; Sarah fallback appeared only as its intended card image, not as every missing-image character. The first 12 Feed images had `loading="eager"`, later images stayed `lazy`, and Chrome console warnings/errors were `[]`. Targeted Playwright Feed E2E also passed without the previous Sarah LCP warning.
21. Feed remix gallery lineage: `feed-remix-gallery-provenance.png`
    Health: pass. Chrome seeded temporary public Feed item `chrome-lineage-1783087250366-character`, opened `/feed?item=character%3Achrome-lineage-1783087250366-character`, verified the focused shared card, clicked Remix from that card, landed on `/generate` with the matching `characterId` and `remixFeedItemId`, submitted Generate, waited for completion, and saw the Gallery card link `Remixed from Feed: Chrome Lineage chrome-lineage-1783087250366` with href `/feed?item=character%3Achrome-lineage-1783087250366-character`. Clicking the badge returned to the focused Feed card with `Showing shared dream.` visible. Chrome console warnings/errors were `[]`; fixture cleanup deleted 1 job, 1 media asset, the temporary character, and owner.
22. Feed collection item type: `feed-collection-card-focused.png`
    Health: pass. Chrome seeded temporary public collection `chrome-feed-collection-1783088921165`, opened `/feed?item=collection%3Achrome-feed-collection-1783088921165`, verified the focused collection card, `Showing shared dream.`, `Shared dream`, loaded eager preview image, Share status `Share link copied.`, and Report status `Report submitted.`. DB verification before cleanup showed one `contentReport` and one `moderationEvent` for target `feed_item / collection:chrome-feed-collection-1783088921165`; fixture cleanup returned 0 remaining fixture rows. Chrome console warnings/errors were `[]`.
23. Explore age filter: `explore-age-filter.png`
    Health: pass. Chrome seeded temporary public Explore characters aged 22, 30, and 38, opened `/?q=Chrome%20Age&gender=any&age_min=25&age_max=34&limit=6`, verified only the age-30 card was visible and the Age filter displayed `25-34`, clicked the Age filter once, verified the URL changed to `age_min=35`, the control displayed `35+`, and only the age-38 card was visible. Fixture cleanup deleted the temporary characters and owner. Chrome console warnings/errors were `[]`.
24. Chat image -> Generate handoff: `chat-image-generate-handoff.png`
    Health: pass. Chrome opened a Melissa chat session, verified the chat header Generate link used `/generate?characterId=melissa-burke`, seeded a completed chat image attachment for the visible session, and reloaded the chat. The completed image rendered with follow-up controls and one `Open in Generate` link. Clicking `Open in Generate` landed on `/generate?characterId=melissa-burke`, the generator Character selector value was `melissa-burke`, and Chrome console errors were `[]`. Attachment action availability was re-audited in step 72.
25. Generate mode preset round-trip: `generate-mode-preset-roundtrip.png`
    Health: pass. Chrome verified the rebuilt PM2 `/generate` bundle exposes `Mode preset` with `Anime` and `Realistic` built-in options. It selected `Realistic` plus `Background=Studio`, saved `Chrome Mode Preset 1783091654232`, applied that saved preset after clearing the selects, and confirmed the controls restored to `Mode preset=seed-preset-mode-realistic` and `Background=seed-preset-background-studio`. Chrome console errors were `[]`; the temporary saved preset was deleted after capture.
26. Generate Gallery download feedback: `generate-gallery-download-status.png`
    Health: pass. Chrome opened the rebuilt PM2 `/generate` bundle, found 6 Gallery image cards, clicked `Download` on media `chrome-profile-search-1783072875011`, verified visible status `Download started.`, confirmed the page stayed on `/generate`, saw no `Download failed.`, and Chrome console errors were `[]`.
27. Generate Image Edit source variation: `generate-image-edit-queued.png`
    Health: pass. Chrome opened the rebuilt PM2 `/generate` bundle, verified the Presets/Image Edit mode switch, selected Image Edit, saw 6 source image cards, confirmed `Create edit` was disabled before source selection, selected media `chrome-profile-search-1783072875011`, confirmed the source stayed selected and `Create edit` enabled, queued the edit, saw visible status `Image edit queued.`, remained on `/generate` with Active Jobs visible, and Chrome console errors were `[]`.
28. Generate Community preset source: `generate-community-preset-source.png`
    Health: pass. Chrome opened the rebuilt PM2 `/generate` bundle, confirmed the Background preset control exposed the seeded public Community preset `Community · Neon Rooftop`, selected `seed-preset-background-neon-rooftop`, kept Active Jobs and Generate controls visible, and Chrome console errors were `[]`. Temporary E2E-created community preset rows were archived after capture.
29. Generate Video disabled surface: `generate-video-disabled-current.png`
    Health: pass. Chrome opened the rebuilt PM2 `/generate` bundle with `video_gen=false` current scope, confirmed there were no Generate `Video` mode buttons, no `Video Beta` text, and no Gallery `Videos` tab. Gallery tabs were exactly `Images` and `Liked`; Active Jobs and the image Generate controls stayed visible. A footer/content `/videos` route link is still present as a valid content index link, not a generator mode or gallery dead entry. Chrome console warnings/errors were `[]`.
30. Launch web-surface smoke: `launch-home-current.png`, `launch-admin-protected-current.png`
    Health: pass. Chrome opened `http://127.0.0.1:3000/` and confirmed the home/explore surface rendered with brand, primary discovery content, no Next/error shell, and Chrome console warnings/errors `[]`. Chrome then opened `http://127.0.0.1:3001/admin`; the first capture was rejected because it still showed a loading skeleton, so the audit waited for hydration and recaptured the loaded admin dashboard with sidebar navigation, metrics, feature flags, no Next/error shell, and Chrome console warnings/errors `[]`.
31. Admin Content Ops production-to-placement: `admin-content-production-reviewing.png`, `admin-content-production-approved.png`, `admin-content-asset-library-approved.png`, `admin-content-placement-published.png`, `admin-content-placement-public-character.png`
    Health: pass. Chrome created one Production Studio batch `Chrome Content Ops 1783097136351`, waited for the local image worker to complete, approved the generated item, verified the asset in Asset Library, created a published `character_avatar` placement for target character `cmr4u6ms9000ludl7p8t0t6zs`, and confirmed the public character detail page rendered the same `/user-content/.../content.png` asset at 512x640. DB verification showed `ContentProductionBatch.status=completed`, item `status=published`, `MediaAssetPlacement.status=published`, target character `imageAssetId=media_ojpc8dekljjmr5616mu`, and audit logs for `content.production.batch.create`, `content.production.item.approve`, and `content.placement.publish`; Chrome console errors were `[]`. A separate refresh probe batch `Chrome Refresh Probe 1783097909176` proved the Production Studio `Refresh` button updates queued UI to `reviewing` after the DB has completed; the temporary probe item was rejected through the UI and had Chrome console errors `[]`.
32. Help Desk appeal -> admin appeal decision -> restored character: `helpdesk-appeal-resolution-submitted.png`, `admin-appeal-overturn-open.png`, `admin-appeal-overturn-modal.png`, `admin-appeal-overturn-resolved.png`, `appeal-restored-character-detail.png`
    Health: pass. Chrome submitted a Help Desk appeal for removed character `chrome-appeal-character-1783098987881-249664`, received `Appeal cmr574bv5001hhgl75ehr16uw submitted.`, verified the appeal appeared in `/admin/moderation` with `Uphold`, `Overturn`, and `Modify` row actions, opened the `Overturn` confirmation, submitted reason plus `OVERTURN`, and verified the appeal row left the open queue without page errors. DB verification showed `Appeal.status=overturned`, `reviewerId=seed-admin-user`, `resolvedAt` set, target character restored to `status=approved`, and `safety.appeal.decision` audit with `targetRestored=true`. Chrome then opened `/characters/chrome-appeal-character-1783098987881-249664` and verified the restored public character detail rendered with Chat/Like/Report actions. Fixture cleanup deleted the temporary appeal, owner, character, audit row, and analytics event after screenshots were captured.
33. Explore content-backed category chips: `explore-content-backed-category-chips.png`, `explore-content-backed-category-filter.png`
    Health: pass. Chrome opened `/explore` after the PM2 production-bundle rebuild and verified the category chips are backed by public approved character counts: `All`, `Slow Burn`, and `Elf` were visible while empty current-beta chips such as `Group Chats` were absent. Clicking `Slow Burn` synced the URL to `/explore?tags=slow-burn`, rendered the matching `Kennedy Graham` card, kept `Group Chats` absent, and Chrome console errors were `[]`.
34. Chat voice playback: `chat-voice-before-play.png`, `chat-voice-playing.png`, `chat-voice-ended.png`
    Health: pass. Chrome opened a seeded Melissa chat session after the PM2 production-bundle rebuild, verified the assistant turn exposed `Play voice`, clicked it, and confirmed the control entered a visible active state with `aria-pressed=true`, `data-state=playing`, and the pink stop affordance. DB verification before cleanup showed one `audio/wav` `MediaAsset` at `/api/v1/media/media_voice_qmig6b2zqgmr58drxz/content` with `messageId=chrome-voice-assistant-1783100955772-39644`, `sessionId=chrome-voice-session-1783100955772-39644`, `durationMs=11520`, and `costDreamcoins=0`; no dreamcoin ledger row was created. After the clip naturally ended, the control returned to idle `Play voice`; Chrome console errors were `[]`. Fixture cleanup deleted the temporary voice media asset, blob file, chat messages, and chat session.
35. Generate low-balance and recovery states: `generate-insufficient-balance.png`, `generate-recovery-states.png`
    Health: pass. Chrome signed up disposable user `chrome-generate-recovery-1783102589677-832512@test.local`, reduced the balance to 0 coins, seeded one failed job and one blocked job, and opened `/generate` after the PM2 production-bundle rebuild. The Generate button was disabled, the upgrade CTA showed `Need 5 coins · you have 0.` with href `/upgrade`, the failed job showed `Failed: provider_timeout`, one `Retry` button, and copy `Retry will reserve the normal cost again.`, and the blocked job showed `Blocked: request_blocked`, no Retry action, plus `Get help` linking to `/helpdesk`. Chrome console errors were `[]`. Fixture cleanup deleted the temporary user and fixture rows.
36. Chat free daily quota boundary: `chat-quota-upgrade-boundary.png`
    Health: pass. Chrome signed up disposable user `chrome-chat-quota-1783103338272-400838@test.local`, seeded today's chat usage to the free cap of 30 messages, opened Melissa Burke chat, typed `chrome quota draft 1783103380100`, and submitted once. The UI showed `Daily free message limit reached.` plus `Upgrade for unlimited messages` linking to `/upgrade`, preserved the exact draft in the message input, and did not persist the draft as a sent user message. Chrome console errors were `[]`. Fixture cleanup deleted the temporary user plus chat usage/session rows.
37. Profile notification preference persistence: `profile-notification-preferences-persisted.png`
    Health: pass. Chrome signed up disposable user `chrome-profile-prefs-1783103878454-785952@test.local`, opened `/profile/notifications`, verified `Product updates` defaulted to checked, unchecked it, saved preferences, reloaded the page, and verified the checkbox stayed unchecked. DB verification before cleanup showed `user_preferences.notificationSettings.productUpdates=false`; fixture cleanup returned 0 remaining user rows. Chrome console errors were `[]`.
38. Global header search from app pages: `global-header-search-melissa-results.png`
    Health: pass. Chrome signed up disposable user `chrome-global-search-1783104222058-331361@test.local`, opened `/generate`, filled the app-shell searchbox with `Melissa Burke`, pressed Enter, and landed on `/?q=Melissa+Burke`. The Explore surface rendered the Melissa Burke result card with href `/characters/melissa-burke`, the search field displayed `Melissa Burke`, and Chrome console errors were `[]`. Fixture cleanup returned 0 remaining user rows.
39. My AI deferred group chats and packs tabs: `my-ai-group-chats-empty-current.png`, `my-ai-packs-empty-current.png`
    Health: pass. Chrome signed up disposable user `chrome-my-ai-tabs-1783104549988-793605@test.local`, opened `/custom`, and verified the My AI tab bar exposes Group chats and Packs without pretending either feature is live. Group chats showed `Group chats are not in this beta`, supporting copy directing users to one-on-one companion chat, and a Create CTA with href `/create`. Packs showed `Packs are not in this beta`, supporting copy explaining saved bundles are deferred, and a Create CTA with href `/create`. Chrome console errors were `[]`; fixture cleanup returned 0 remaining user rows.
40. Age gate fresh visitor and accepted workspace transition: `age-gate-fresh-generate-blocked-current.png`, `age-gate-accepted-generate-current.png`
    Health: pass. Chrome opened the same PM2 main service through fresh local origin `http://fresh-age-gate.localhost:3000/generate` so existing `127.0.0.1` / `localhost` cookies could not mask the first-visit state. Before acceptance, the page showed only the age gate with `Adults Only`, Terms href `/terms`, Leave site href `https://www.google.com/`, one `I'm over 18` button, no `<main>`, and no character links. After clicking the button, Chrome stayed on `/generate`, the gate disappeared, the anonymous Generate workspace rendered with `0 coins`, Active Jobs, Gallery, Login/Join Free, disabled Generate due to insufficient balance, and console errors were `[]`.
41. Chat message report: `chat-message-report-submitted-current.png`
    Health: pass. Chrome signed up disposable user `chrome-chat-report-1783105499532-86214@test.local`, opened Melissa Burke chat, sent `chrome report message 1783105499532`, clicked the scoped `Report message` action on user message `msg_92c7039f9cb54ed2943cb305a8023aa7`, and saw `Report submitted.`. DB verification before cleanup found `ContentReport cmr5az6o3003e59l7w1nmphb1` with `targetType=chat_message`, category `other_prohibited_content`, description `Chat message report`, status `open`, priority `3`, plus one `community_report` moderation event with status `flagged`. Fixture cleanup returned 0 remaining user, report, moderation-event, chat-session, and chat-message rows. Chrome console errors were `[]`.
42. Profile account management sign-out and deletion: `profile-account-management-current.png`, `account-delete-login-blocked-current.png`
    Health: pass. Chrome signed up disposable user `chrome-account-current-1783105842408-379197@test.local`, opened `/profile/account-management`, verified the Account management panel, `Sign out all sessions`, Delete confirmation input, and disabled Delete button. `Sign out all sessions` redirected to `/login`; the same account could log back in. Delete stayed disabled for empty/`NOPE`, enabled only after typing `DELETE`, then account deletion redirected to `/login`. A follow-up login attempt showed `Account is not active`. DB verification showed `User.status=deleted`, `deletedAt=2026-07-03T19:10:44.982Z`, `sessionCount=0`, and completed `chat.inbound` `user.deleted` job. Fixture cleanup removed the disposable user, session/account/ledger/age rows, and queued job; remaining counts were 0. Chrome console errors were `[]`.
43. Character detail current smoke: `character-detail-melissa-current.png`
    Health: pass. Chrome opened `/characters/melissa-burke` on a local origin with the gate already accepted from the age-gate evidence run, verified the Melissa Burke detail page rendered without the age gate, and confirmed the visible loaded character image plus Chat, Like, and Report controls. The screenshot shows the detail layout, sidebar navigation, footer links, and primary Chat/Like/Report actions. Page evaluation found 2 visible images, 2 loaded images, heading `Melissa Burke`, and Chrome console errors were `[]`.
44. Terms pre-gate policy index: `terms-policy-index-current.png`
    Health: pass after fix. Chrome first exposed that a fresh-origin `/terms` visit still showed the age gate, making the gate's own Terms link loop back into the gate instead of a readable policy page. `AgeGateBoundary` now bypasses the client gate for `/terms` and `/safety/*` while leaving app/API gated surfaces unchanged. After rebuilding and restarting `main-web`, Chrome opened `http://fresh-terms-fixed.localhost:3000/terms` with no age-gate acceptance, saw `Terms & Policies`, `12 policy routes`, all 12 expected Safety Center policy links, all 4 account/support action links, no age gate, and Chrome console errors `[]`. Focused Playwright also verifies `/safety/policies/acceptable-use` is readable before gate acceptance and `/generate` remains blocked.
45. Character detail report -> admin moderation queue: `character-report-submitted-current.png`, `admin-moderation-character-report-current.png`
    Health: pass after fix. Chrome opened `/characters/melissa-burke`, clicked the single visible `Report` button, saw `Report submitted for review.`, and saved the submitted-state screenshot with console errors `[]`. DB verification showed `content_reports` and `moderation_events` for `character / melissa-burke` both incremented from 81 to 82, with new report `cmr5byn530001dpl7wz9goajv` and a linked `community_report` moderation event. The admin queue API returned that same report by id. Chrome then exposed that the admin Reports table sorted low-priority reports oldest-first and only loaded the first 100 rows, hiding fresh ordinary reports during backlog. The queue now keeps priority first but sorts same-priority reports newest-first, has focused Vitest coverage for >100 same-priority reports, and after rebuilding/restarting `main-web` plus `admin-web`, Chrome verified `/admin/moderation` shows `cmr5byn530001dpl7wz9goajv` as the first Reports row with console errors `[]`.
46. Chat hub session list and return-to-session: `chat-hub-session-open-current.png`, `chat-hub-session-list-current.png`, `chat-hub-returned-session-current.png`
    Health: pass. Chrome used fresh local origin `http://fresh-chat-hub.localhost:3000`, accepted the local gate, signed up disposable user `chrome-chat-hub-1783107825234@test.local`, opened Melissa Burke, clicked `Chat`, and landed on `/chat/sess_49faf1b6234f4e5087e5aca85b390827`. The new session rendered the Melissa header, active memory state, Generate handoff, message composer, send button, and sidebar active state with console warnings/errors `[]`. Chrome then opened `/chat`, saw the authenticated Chat hub with `Your chats`, the user name, logout, Explore CTA, and exactly one `data-testid=chat-hub-session` card linking to the created session. Clicking that card returned to the same session URL with Melissa visible and the composer present. Cleanup removed the temporary chat-service session, main `recent_chats` projection, age-gate row, and user; verification counts returned 0.
47. Creator profile follow and first-row card images: `creator-profile-community-entry-current.png`, `creator-profile-before-follow-current.png`, `creator-profile-followed-current.png`, `creator-profile-character-detail-current.png`
    Health: pass after fix. Chrome signed up disposable user `chrome-creator-profile-1783108484645@test.local`, opened Community, navigated from `@some1cool` to `/creators/seed-creator-some1cool`, verified 3 public character cards, clicked Follow, saw `Following` and followers increase from 2 to 3, and clicked Melissa Burke back to `/characters/melissa-burke`. Chrome exposed that the followed/reloaded creator profile could leave first-row Next-optimized card images visually stuck on placeholders even though DOM image resources reported loaded; creator profile now renders the first 5 card images eager and unoptimized/direct public paths. After rebuilding/restarting `main-web`, Chrome verified the first-row images render from `/images/ourdream/*.webp`, console errors `[]`, DB had one follow row before cleanup, and cleanup returned 0 user/follow/age/analytics/ledger/session/account rows.
48. Admin Users/Billing high-risk operator loop: `admin-users-fixed-target-current.png`, `admin-user-suspended-fixed-current.png`, `admin-user-restored-fixed-current.png`, `admin-billing-adjust-filled-fixed-current.png`, `admin-billing-adjusted-cleared-current.png`, `admin-audit-log-user-billing-fixed-current.png`
    Health: pass after fix. Chrome used admin `seed-admin-user` on `/admin/users`, seeded disposable target `chrome-admin-billing-fixed-1783110240544-915165`, filtered the target row, suspended it through the confirmation modal, restored it through the confirmation modal, then used `/admin/billing` to append a +37 `admin_adjust` ledger entry. Chrome initially exposed a duplicate-adjustment risk: after success, the adjustment form retained the target user and delta. The admin action handler now shows a success status and clears the billing adjustment inputs after a successful adjustment; the adjustment inputs also have accessible labels. Chrome verified the success banner, cleared inputs, ledger row, and `/admin/audit-log` rows for `billing.ledger.adjust` plus both `user.status.write` actions. DB verification matched final user `active`, one +37 ledger entry, and three audit rows before cleanup; cleanup returned 0 target user/ledger/audit rows. Chrome console errors were `[]`.
49. Admin global filter accessible name: `admin-filter-aria-zh-current.png`
    Health: pass after fix. Chrome opened `/admin/users` after rebuilding and restarting `admin-web`, confirmed the localized filter input exposes `aria-label="筛选"` in Chinese, matched exactly one `textbox` named `筛选`, filled it with `seed-admin-user`, and saw Chrome console errors `[]`. The focused admin-web E2E now switches the admin locale to Chinese, asserts the same accessible name, and restores English before continuing.
50. Safety Center header/footer dead-control cleanup: `safety-policy-no-dead-header-current.png`
    Health: pass after fix. Chrome opened `/safety/policies/acceptable-use` after rebuilding and restarting `main-web`, verified the policy page renders without the age gate, the current `Acceptable use` nav link exposes `aria-current="page"`, the removed header Search copy count is 0, `Toggle dark mode` button count is 0, `mintlify` copy count is 0, footer branding shows `ourdream.ai Trust & Safety`, and Chrome console errors were `[]`. Focused public-route E2E also verifies the mobile Safety Navigation menu opens, marks `Acceptable use` as current, links to `Appeals`, and keeps the dead header controls absent.
51. Help Desk support-link semantics: `helpdesk-support-link-semantics.png`
    Health: pass after fix. Chrome opened `/helpdesk` after rebuilding and restarting `main-web`, verified the three Help Desk support cards render with distinct navigation semantics: `Account & billing` and `Trust contact` are `data-link-kind=internal`, have no `target`, and show one `ArrowRight` icon each; `Discord` is `data-link-kind=external`, uses `target="_blank"` with `rel="noopener noreferrer"`, and shows one external-link icon. Chrome console warnings/errors were `[]`. Focused public-route E2E now locks the same internal/external card semantics.
52. Global sidebar/footer external-link semantics: `global-link-semantics-current.png`
    Health: pass after fix. Chrome opened `/helpdesk` on the rebuilt PM2 `main-web` service and verified the global desktop sidebar plus footer link contract. Sidebar `Discord` is `data-link-kind=external`, uses `target="_blank"` with `rel="noopener noreferrer"`, and shows one external-link icon; sidebar `Help Desk` remains `data-link-kind=internal` with no `target`. Footer `Help Centre`, `Affiliates`, `Discord`, `Reddit`, and `Twitter / X` are external with `target="_blank"` and `rel="noopener noreferrer"`, while footer `Help Desk` remains internal with no `target`. Chrome console warnings/errors were `[]`; screenshot inspection confirmed the Help Desk page rendered normally with the external-link indicator visible in the sidebar.
53. Announcement banner link semantics and Explore LCP eager range: `announcement-banner-link-semantics.png`
    Health: pass after fix. Chrome opened the current dev bundle at `http://127.0.0.1:3070/` with a temporary active announcement. The banner rendered at the top of the app shell, its `Learn more` link was `data-link-kind=external`, `href=https://help.ourdream.ai/`, `target="_blank"`, and `rel="noopener noreferrer"`, and the external-link icon was visible. Page evaluation also confirmed the first 10 character-card images on the Explore surface now use `loading="eager"`, covering the desktop first viewport; Chrome console warnings/errors were `[]`. The temporary announcement row was removed after capture.
54. Explore filter select menus: `explore-filter-select-menus.png`
    Health: pass after fix. Chrome opened the current dev bundle at `http://127.0.0.1:3073/?gender=any&age_min=25&age_max=34&limit=4` and verified Gender, Style, and Age are three unique native `SELECT` controls instead of pseudo-dropdown cycle buttons. Page evaluation confirmed the full option sets: Gender `female/male/trans/any`, Style `any/realistic/anime/hybrid`, and Age `any/18-24/25-34/35+`. Selecting Gender `male`, Style `anime`, and Age `35+` synced the URL to `gender=male`, `style=anime`, and `age_min=35`; final control values matched the selected options, and Chrome console warnings/errors were `[]`.
55. Mobile Explore filter visibility and desktop regression: `mobile-explore-controls-current.png`, `mobile-explore-controls-fixed-clean.png`, `explore-desktop-filter-layout-after-mobile-fix.png`
    Health: pass after fix. The 390px mobile supplement first exposed that the Age filter was partly outside the viewport (`right ~= 451px`), requiring horizontal scroll to complete the primary Explore filter row. `TopControls` now uses a two-row mobile control layout with a three-column Gender/Style/Age filter grid. Clean 390px evidence after a cold reload shows Gender/Style/Age right edges at about `127px`, `255px`, and `382px`, bottom nav fixed, sidebar hidden, two-column cards visible, and console errors `0`. Chrome plugin viewport control is not available in this environment, so the 390px proof is Playwright supplemental evidence; Chrome direct regression after the fix verified the 1512px desktop layout still has sidebar visible, mobile nav hidden, stable select positions, URL sync for `gender=male`, `style=anime`, and `age_min=35`, and console warnings/errors `[]`.
56. Create anonymous signup return: `create-flow-auth-required-current.png`, `create-flow-anon-next-redirect-current.png`, `create-flow-after-signup-current.png`, `create-flow-anon-draft-before-next-final.png`, `create-flow-signup-next-final.png`, `create-flow-returned-after-signup-final.png`
    Health: pass after fix. Chrome exposed that the low-friction Create funnel let anonymous users start the builder, but clicking `Next` redirected to bare `/signup`; after signup, the user landed on Explore `/` instead of returning to Create, losing the creation task context. `CreateWorkspace` now redirects logged-out draft writes to `/signup?next=/create`, `AuthWorkspace` accepts only safe internal `next` targets, and `/login`/`/signup` no longer mis-highlight Explore in the sidebar. Final Chrome evidence on a fresh origin shows the draft name `Chrome Final 1783117004732`, signup URL `http://fresh-create-final.localhost:3076/signup?next=%2Fcreate`, Explore without the active background token, post-signup URL `/create`, the same draft name restored, `Log out` visible, and Chrome console warnings/errors `[]`. Focused E2E covers the same anonymous draft -> signup -> return-to-builder path plus default signup/login regression.
57. Upgrade anonymous signup return: `upgrade-anon-plans-before-current.png`, `upgrade-anon-checkout-error-current.png`, `upgrade-anon-plans-before-final.png`, `upgrade-anon-signup-next-final.png`, `upgrade-anon-returned-after-signup-final.png`
    Health: pass after fix. Chrome exposed that an anonymous visitor could open `/upgrade` and click `Demo upgrade`, but the checkout POST returned a visible dead-end `Unauthorized` status on the same page with no signup recovery. `UpgradeWorkspace` now sends 401 checkout attempts to `/signup?next=/upgrade?plan=premium&billing=monthly`, preserving the selected plan intent without auto-purchasing. Final Chrome evidence on a fresh origin shows the Premium monthly click reached the signup URL with the encoded `next`, signup returned to `http://fresh-upgrade-final.localhost:3077/upgrade?plan=premium&billing=monthly`, `Log out` was visible, the Premium monthly `Demo upgrade` action was still available, the demo checkout notice stayed visible, the sidebar had no mistaken Explore active state, and Chrome console warnings/errors were `[]`. Focused E2E covers the same anonymous checkout intent -> signup -> returned plans path plus the existing signed-in activation flow.
58. Character detail anonymous signup return: `character-anon-detail-before-current.png`, `character-anon-chat-signup-bare-current.png`, `character-anon-after-signup-home-current.png`, `character-anon-detail-before-final.png`, `character-anon-chat-signup-next-final.png`, `character-anon-returned-after-signup-final.png`, `character-anon-chat-open-after-return-final.png`
    Health: pass after fix. Chrome exposed that an anonymous visitor on `/characters/melissa-burke` who clicked `Chat` was sent to bare `/signup`; after signup, the user landed on Explore `/`, losing both the character detail context and the chat intent. Character detail 401 actions now route to `/signup?next=/characters/melissa-burke`, and Auth accepts safe internal `/characters/*` returns. Final Chrome evidence on a fresh origin shows the Chat click reached the encoded signup URL, signup returned to `/characters/melissa-burke`, the Melissa detail page remained visible, a second Chat click opened `/chat/sess_7bcebcd77e7843b1956c2ce122c9bb73`, and Chrome console warnings/errors were `[]`. Focused E2E covers the anonymous character detail Chat -> signup -> returned detail -> chat session path plus the existing signed-in character-detail chat flow.
59. Feed anonymous Chat signup return: `feed-anon-chat-before-current.png`, `feed-anon-chat-signin-deadend-current.png`, `feed-anon-chat-before-final.png`, `feed-anon-chat-signup-next-final.png`, `feed-anon-chat-returned-character-final.png`, `feed-anon-chat-open-final.png`
    Health: pass after fix. Chrome exposed that an anonymous visitor on `/feed` who clicked the Melissa Burke card `Chat` stayed on `/feed` with only `Sign in to start chat.`, leaving the user to manually find signup and then rediscover the card. Feed character-card Chat 401s now route to `/signup?next=/characters/melissa-burke`, which preserves the chat target as a role detail page rather than a fragile feed scroll position. Final Chrome evidence on a fresh origin shows the Chat click reached the encoded signup URL, signup returned to `/characters/melissa-burke`, a second Chat click opened `/chat/sess_fab259e59e1740f58cabc7e47625cc6c`, the message composer was visible, and Chrome console warnings/errors were `[]`. Focused E2E covers the anonymous Feed Chat -> signup -> returned detail -> chat session path plus the existing signed-in Feed share/report/remix flow.
60. Generate app-shell anonymous signup return: `generate-anon-before-join-current.png`, `generate-anon-signup-bare-current.png`, `generate-anon-after-signup-home-current.png`, `generate-anon-before-join-final.png`, `generate-anon-signup-next-final.png`, `generate-anon-returned-after-signup-final.png`
    Health: pass after fix. Chrome exposed that a fresh anonymous visitor on `/generate` saw the app-shell `Join Free` CTA as bare `/signup`; after signup the user landed on Explore `/`, losing the generator context. `AuthNav` now shares the same safe internal redirect helper as `AuthWorkspace`, wraps the `useSearchParams` path in `Suspense`, and emits `/signup?next=/generate` plus `/login?next=/generate` for allowed app paths. Final Chrome evidence on a fresh origin shows `Join Free` had href `/signup?next=%2Fgenerate`, signup URL carried `next=/generate`, signup returned to `/generate`, `Log out` appeared after auth state resolved, the Generate control stayed visible, and Chrome console warnings/errors were `[]`. Focused E2E covers the same `/generate` app-shell signup return and the existing global header search flow.
61. Profile and Chat hub auth-card signup returns: `profile-anon-gate-before-current.png`, `profile-anon-signup-bare-current.png`, `profile-anon-after-signup-home-current.png`, `profile-anon-gate-before-final.png`, `profile-anon-signup-next-final.png`, `profile-anon-returned-after-signup-final.png`, `chat-hub-anon-gate-before-final.png`, `chat-hub-anon-signup-next-final.png`, `chat-hub-anon-returned-after-signup-final.png`
    Health: pass after fix. Chrome exposed that `/profile` had the correct app-shell `Join Free` href but the in-page `Sign in to open My AI` card still linked to bare `/signup`; signup then landed on Explore `/` instead of returning to My AI. Profile and Chat hub signed-out cards now use the same safe auth href helper as the app shell. Final Chrome evidence shows Profile card links `/login?next=%2Fprofile` and `/signup?next=%2Fprofile`, signup returns to `/profile` with `Log out`, balance, billing, and library tabs visible; Chat hub card links `/login?next=%2Fchat` and `/signup?next=%2Fchat`, signup returns to `/chat` with the empty chat hub visible. Chrome console warnings/errors were `[]`. Focused E2E covers both in-page signed-out cards.
62. Creator profile anonymous Follow signup return: `creator-follow-anon-before-current.png`, `creator-follow-anon-silent-deadend-current.png`, `creator-follow-anon-before-final.png`, `creator-follow-signup-next-final.png`, `creator-follow-returned-after-signup-final.png`, `creator-follow-following-final.png`
    Health: pass after fix. Chrome exposed that a fresh anonymous visitor on `/creators/seed-creator-some1cool` could click `Follow`, but the button silently rolled back to `Follow`, stayed on the creator page, and displayed no sign-in recovery copy. Creator profile Follow 401s now redirect to `/signup?next=/creators/<id>`, and Auth accepts safe internal `/creators/*` returns. Final Chrome evidence on a fresh origin shows Follow reached the encoded signup URL, signup returned to `/creators/seed-creator-some1cool`, a second Follow changed the button to `Following`, and Chrome console errors were `[]`. Focused E2E covers anonymous creator Follow -> signup -> returned creator -> Following plus the existing signed-in Community -> creator follow/reload path.
63. Community anonymous Follow signup return: `community-follow-anon-before-current.png`, `community-follow-anon-signin-deadend-current.png`, `community-follow-anon-before-final.png`, `community-follow-signup-next-final.png`, `community-follow-returned-creator-final.png`, `community-follow-following-final.png`
    Health: pass after fix. Chrome exposed that a fresh anonymous visitor on `/community` who clicked the `@some1cool` Dreamer card `Follow` stayed on `/community` with only `Sign in to follow creators.`, leaving the user to manually find signup and rediscover the creator. Community Dreamer and Character-card Follow 401s now redirect to `/signup?next=/creators/<creatorId>`. Final Chrome evidence on a fresh origin shows the Community Follow click reached the encoded signup URL, signup returned to `/creators/seed-creator-some1cool`, a second Follow changed the button to `Following`, and Chrome console errors were `[]`. Focused E2E covers anonymous Community Follow -> signup -> returned creator -> Following plus the existing signed-in Community report/follow path.
64. Help Desk anonymous support-request signup return: `helpdesk-anon-support-before-current.png`, `helpdesk-anon-support-signin-deadend-current.png`, `helpdesk-anon-support-before-final.png`, `helpdesk-anon-support-signup-next-final.png`, `helpdesk-anon-support-returned-draft-final.png`, `helpdesk-anon-support-submitted-final.png`
    Health: pass after fix. Chrome exposed that a fresh anonymous visitor could fill the Help Desk support form and click `Submit request`, but the page stayed on `/helpdesk` with only `Sign in to submit a tracked support request.`, leaving the user to manually find signup and risk losing the support context. Help Desk support-request 401s now save the category/subject/details/diagnostic consent draft locally, redirect to `/signup?next=/helpdesk`, restore the draft after signup, and clear it after a tracked `SUP-*` ticket is created. Clean final Chrome evidence on `fresh-helpdesk-support-clean.localhost:3084` shows signup returned to `/helpdesk`, category `chat` plus subject/details restored, `Support request SUP-PG19PQ88D4 received.`, empty form after submit, and Chrome console errors `[]`. Focused E2E covers the anonymous Help Desk support draft -> signup -> restored form -> tracked support ticket path plus the existing signed-in support/roadmap/appeal workflow.
65. Help Desk anonymous roadmap and appeal signup returns: `helpdesk-anon-feedback-before-current.png`, `helpdesk-anon-feedback-signin-deadend-current.png`, `helpdesk-anon-feedback-before-final.png`, `helpdesk-anon-feedback-signup-next-final.png`, `helpdesk-anon-feedback-returned-draft-final.png`, `helpdesk-anon-feedback-submitted-final.png`, `helpdesk-anon-appeal-signin-deadend-current.png`, `helpdesk-anon-appeal-before-final.png`, `helpdesk-anon-appeal-signup-next-final.png`, `helpdesk-anon-appeal-returned-draft-final.png`, `helpdesk-anon-appeal-submitted-final.png`
    Health: pass after fix. Chrome exposed that anonymous roadmap idea and appeal submissions stayed on `/helpdesk` with inline sign-in copy, leaving users to manually find signup and risking loss of the detailed idea/appeal context. Help Desk now saves roadmap and appeal drafts locally on 401, redirects to `/signup?next=/helpdesk`, restores each draft after signup, shows restored-draft status, and clears the corresponding form after successful submission. Final Chrome evidence on fresh origins shows roadmap category/title/details restored, `Feature idea submitted and your vote was counted.`, appeal target type/id/decision/details restored, `Appeal ... submitted.`, reset forms after submit, and Chrome console errors `[]`. Focused E2E covers anonymous support, roadmap, and appeal drafts through signup with DB assertions for `SupportRequest`, `ProductFeedbackItem`/auto-vote, and `Appeal`.
66. Feed anonymous Like signup return: `feed-anon-like-before-current.png`, `feed-anon-like-deadend-current.png`, `feed-anon-like-before-final.png`, `feed-anon-like-signup-next-final.png`, `feed-anon-like-returned-focused-final.png`, `feed-anon-like-liked-final.png`
    Health: pass after fix. Chrome exposed that an anonymous visitor on `/feed` who clicked the Melissa Burke card `Like` stayed on `/feed` with only an `Unauthorized` status and the button rolled back to `Like`. Feed card Like 401s now redirect to `/signup?next=/feed?item=character%3Amelissa-burke`, signup returns to the focused Melissa feed card with `Showing shared dream.`, and an explicit second Like persists as `Liked` with `aria-pressed=true`. Final Chrome evidence on a fresh origin shows the full return path and Chrome console errors were `[]`. Focused E2E covers anonymous Feed Like -> signup -> focused feed item -> persisted `CharacterLike` plus the existing Feed Chat and share/report/remix flows.
67. Generate anonymous My Presets signup return: `generate-anon-preset-selected-current.png`, `generate-anon-preset-auth-deadend-current-full.png`, `generate-anon-preset-selected-final.png`, `generate-anon-preset-signup-next-final.png`, `generate-anon-preset-returned-draft-final.png`, `generate-anon-preset-saved-final.png`
    Health: pass after fix. Chrome exposed that an anonymous `/generate` visitor could select `Background=Studio`, enter a My Presets name, click Save, and remain on `/generate` with raw `Unauthorized`. Generate My Presets 401s now save the selected mode/background/pose/outfit/prompt draft locally, redirect to `/signup?next=/generate`, restore the draft after signup with visible status copy, and clear the draft after the authenticated Save succeeds. Final Chrome evidence on a fresh origin shows signup returned to `/generate`, the preset name and Studio background restored, the second Save displayed `Saved preset "Chrome Preset Final 1783126731447".`, DB confirmed `backgroundPresetId=seed-preset-background-studio`, the disposable user was deleted, and Chrome console errors were `[]`. Focused E2E covers anonymous Generate preset Save -> signup -> restored draft -> persisted preset.
68. Help Desk anonymous roadmap vote signup return: `helpdesk-anon-vote-before-current.png`, `helpdesk-anon-vote-signup-current.png`, `helpdesk-anon-vote-returned-current.png`, `helpdesk-anon-vote-clean-before-final.png`, `helpdesk-anon-vote-clean-signup-next-final.png`, `helpdesk-anon-vote-clean-returned-voted-final.png`
    Health: pass after fix. Chrome exposed that an anonymous visitor could click `Vote` on an existing roadmap item and reach `/signup?next=/helpdesk`, but after signup the exact item still showed `Vote`/`aria-pressed=false`, so the clicked vote intent was lost. Help Desk now stores the pending roadmap vote locally on 401, restores it after signup, replays the vote request once roadmap items load, clears the pending vote after success, and shows `Vote counted.`. Clean final Chrome evidence on `fresh-helpdesk-vote-clean-final.localhost:3088` shows the disposable item `Chrome Clean Vote Return 1783131155711-374411` at `Vote 0`, signup URL `/signup?next=%2Fhelpdesk`, returned Help Desk state with `Vote counted.`, the same card changed to `Voted 1` with `aria-pressed=true`, and DB verification found `ProductFeedbackVote cmr5qaroc000vqal7hl05y5qb` plus `ProductFeedbackItem.voteCount=1` before fixture cleanup. Focused E2E covers anonymous roadmap Vote -> signup -> returned Help Desk -> applied vote and cleared pending localStorage.
69. Character detail contextual Generate handoff: `character-detail-generate-missing-current.png`, `character-detail-generate-cta-final.png`, `character-detail-generate-selected-final.png`, `character-detail-generate-signup-next-final.png`, `character-detail-generate-returned-final.png`
    Health: pass after fix. Chrome exposed that `/characters/melissa-burke` had only Chat/Like/Report in the detail action row; the only Generate links were global nav/sidebar links, so a user could not start generation directly from the selected character. Character detail now exposes a contextual `Generate` CTA with href `/generate?characterId=melissa-burke`. Final Chrome evidence on `fresh-character-generate-final.localhost:3089` shows the detail CTA, `/generate?characterId=melissa-burke`, the Character selector value `melissa-burke`, `Join Free` href `/signup?next=%2Fgenerate%3FcharacterId%3Dmelissa-burke`, signup returning to the same generator URL with Melissa still selected, disposable user cleanup, and Chrome console errors `[]`. Focused E2E covers character detail Generate -> generator selection -> signup -> returned generator selection.
70. Community report and direct filter menus: `community-anon-character-report-submitted-current.png`, `community-filter-cycle-buttons-current.png`, `community-filter-select-menus-final.png`
    Health: pass after fix. Chrome first verified Community character/profile report submission was not a dead end for anonymous visitors; the temporary report rows were removed after verification. The same pass exposed that Community `Release`/`Gender`/`Style` filters were still click-to-cycle `BUTTON`s with no direct option choice. Community now uses native `SELECT` menus styled as product pills. Final Chrome evidence verifies three labeled selects with full option sets, zero legacy cycle filter buttons, successful direct selections for `30d`, `male`, and `realistic`, loaded character and collection cards after returning to a populated filter state, and Chrome console errors `[]`. Focused E2E covers direct select values plus filtered female/male character results and collections.
71. Feed collection View to Community focus: `feed-collection-view-generic-link-current.png`, `feed-collection-view-community-unfocused-current.png`, `feed-collection-view-focused-link-final.png`, `feed-collection-view-community-focused-final.png`
    Health: pass after fix. Chrome exposed that public collection cards in Feed had image and `View` links pointing to generic `/community`, so users lost the selected collection context after clicking. Feed collection image and `View` links now target `/community?collection=<id>`, and Community reads that query, shows `Showing collection: Fantasy Escapes.`, scrolls the matching card into view, and marks it with `data-focused="true"`. Final Chrome verification on `fresh-feed-collection-view-final.localhost:3090` confirmed both Feed links target `/community?collection=seed-collection-fantasy-escapes`, the Community URL keeps that query, the `Fantasy Escapes` card has `data-collection-id="seed-collection-fantasy-escapes"` and `data-focused="true"`, the card is visible in the viewport, and Chrome console errors were `[]`. Focused E2E extends the Feed share/report/remix workflow to cover this collection View handoff before continuing the existing Feed actions.
72. Chat image attachment action availability: `chat-image-actions-current.png`, `chat-image-add-identity-current.png`, `chat-image-actions-official-character-final.png`, `chat-image-more-like-this-queued-final.png`
    Health: pass after fix. Chrome exposed that a completed image attachment in an official Melissa chat session showed `Add identity`, but the action is only valid for user-owned characters and returned the visible error `Couldn't update identity.` with a backend 404. Chat session payloads now include `character.canUpdateIdentity`, and the attachment card hides `Add identity` for official/platform characters while preserving `More like this` and `Open in Generate`. Final Chrome evidence on `fresh-chat-image-actions-current.localhost:3091` shows `Add identity` absent for Melissa, `More like this` still queues a `media_variation` job with the source media id and `characterId=melissa-burke`, and Chrome console errors were `[]`. Focused E2E covers the hidden official-character identity action and the queued variation job.
73. Admin CMS published-page shell: `cms-published-page-shell-current.png`, `cms-published-page-shell-final.png`
    Health: pass after fix. Chrome exposed that a published DB-backed CMS page rendered only the article body, with no sidebar, no top search, no footer, and no mobile navigation markup (`topSearchCount=0`, `sidebarLinks=[]`, `footerText=null`). `CmsRenderer` now reuses the normal product `RouteShell`, keeps CTA hrefs to internal paths or `http(s)` URLs only, and CMS create/update/publish invalidates the CMS path/tag cache. Final Chrome evidence on `http://127.0.0.1:3092/chrome-cms-shell-final-1783136033970` shows sidebar Create/Generate links, top search, footer text, mobile nav markup, the article CTA href `/`, and the same page metadata. Focused E2E covers admin CMS create -> publish -> public route with app shell present.
74. Revenue chain: upgrade -> entitlement -> generation -> gallery delivery: `revenue-generate-anon-before-upgrade.png`, `revenue-generate-free-user-before-upgrade.png`, `revenue-upgrade-premium-activated.png`, `revenue-generate-premium-unlocked.png`, `revenue-generation-complete-gallery.png`
    Health: pass. Chrome used fresh origin `http://fresh-revenue-flow.localhost:3093` to avoid existing cookies. Anonymous `/generate` showed the age gate, then the generator with `0 coins`, disabled Generate, Premium prompt locks, and `/upgrade` recovery. Signup through `/signup?next=/generate` returned to Generate with `250 coins`, Generate enabled, and prompt controls still locked. Entering Upgrade from the locked prompt, Premium monthly demo checkout marked the plan `Current plan`, granted `1,500 dreamcoins`, and exposed `Start generating`. Returning to `/generate` showed `1,750 coins`, unlocked Prompt and Negative Prompt, no Premium lock copy, and a usable Generate button. Submitting one image reserved 5 coins, created job `cmr5tonic000jf1l7pwfgc00u`, completed through the local worker path, refreshed the page state to `Generation complete.`, showed the job as `Completed`, and rendered Gallery media `media_7oocpaiqcugmr5tpigl` from `/user-content/.../content.png`. Chrome console warnings/errors were `[]`; fixture cleanup removed disposable user `chrome-revenue-1783136830632@test.local` and the generation job. Focused E2E now extends the signed-in Upgrade test through generation completion and Gallery `/user-content` delivery.
75. Launch readiness surface and provider-gate adversarial check: `launch-readiness-home-2026-07-04.png`, `launch-readiness-generate-age-gate-2026-07-04.png`, `launch-readiness-generate-accepted-2026-07-04.png`, `launch-readiness-admin-package-protected-2026-07-04.png`
    Health: pass with public-launch caveats. Chrome used fresh origin `http://launch-readiness-1783137908.localhost:3094` for main and `http://launch-admin-1783137908.localhost:3001` for admin. Home rendered Explore cards, filters, and brand content with no Next/error shell and console warnings/errors `[]`. Fresh `/generate` first showed only the age gate; after `I'm over 18`, it rendered the anonymous Generate workspace with `0 coins`, disabled Generate, Login/Join Free, Active Jobs, and Gallery. An initial attempt to treat main `/admin` as the admin surface was rejected because it correctly showed the product 404; the admin package `/admin` on port 3001 rendered the dev-only login wall with no dashboard leakage and console warnings/errors `[]`. Script evidence matched the browser run: `MAIN_WEB_URL=http://launch-readiness-1783137908.localhost:3094 ADMIN_WEB_URL=http://launch-admin-1783137908.localhost:3001 bun run launch:probe:web-surface -- --report .tmp/launch-web-surface-probe-2026-07-04-goal.json` returned `ok=true`. The root launch command was also hardened so `bun run check:launch -- --json` writes parseable JSON directly; it still correctly fails in the current shell at `7 pass / 49 fail / 2 warn`. Fresh mock `payment`/`blob`/`age` probe reports passed individually, but the production-shaped gate still rejected them because providers did not match `btcpay`/`r2`/`gocam`.
76. Production template video-disabled alignment: `generate-video-disabled-template-2026-07-04.png`
    Health: pass. The production env audit found `packages/gen/.env.production.example` still defaulted `GEN_VIDEO_PROVIDER=pipeline` even though the current launch scope keeps `video_gen=false` and main-web's template/runbook already say video must remain disabled. The gen production template now defaults `GEN_VIDEO_PROVIDER=mock`, and launch-readiness tests lock both main and gen production templates to the deferred-video state. Chrome used fresh origin `http://video-disabled-template.localhost:3096/generate`, accepted the age gate, and verified the visible Generate workspace still exposes only image-generation controls: `Presets`, `Image Edit`, Active Jobs, Gallery buttons `Images` and `Liked`, no Video mode button, no `Video Beta`, and no Gallery `Videos` tab. The only `Videos` text on the page is the footer/content route link. Chrome console warnings/errors were `[]`.
77. Payment launch-proof hardening
    Health: pass for gate correctness; blocked for public launch until real BTCPay evidence exists. The adversarial payment audit found that the existing `PAYMENT_PROVIDER_PROBE_REPORT` proved only BTCPay store-read access, which could pass with a key missing `Create invoice` permission. The payment probe now performs the same critical Greenfield action as checkout readiness needs: after reading `/api/v1/stores/{storeId}`, it creates a small `metadata.launchProbe=true` invoice through `/api/v1/stores/{storeId}/invoices` and records `canCreateInvoice`, `invoiceId`, and `checkoutUrl`. Launch readiness now rejects legacy store-read-only reports and requires a public HTTPS checkout URL for `PAYMENT_PROVIDER=btcpay`. Focused tests passed: `probe-payment-provider.test.ts` and `launch-readiness.test.ts` ran 59/59, and the latest local mock report is `.tmp/launch-payment-probe-2026-07-04-invoice-proof.json`.
78. Upgrade anonymous checkout redirect regression: `payment-live-proof/01-upgrade-demo-checkout-anonymous.png`, `payment-live-proof/02-upgrade-anonymous-unauthorized-before.png`, `payment-live-proof/03-upgrade-signup-next-after-fix.png`, `payment-live-proof/04-upgrade-returned-after-signup-after-fix.png`, `payment-live-proof/05-upgrade-premium-activated-after-fix.png`, `payment-live-proof/05-upgrade-premium-activated-after-fix-viewport.png`, `payment-live-proof/06-upgrade-premium-activated-actions-after-fix.png`
    Health: pass after fix. Chrome first verified anonymous `/upgrade` clearly shows `DEMO CHECKOUT`, `No real payment is collected`, and `Demo upgrade` buttons. Clicking Premium monthly then exposed a dead inline `Unauthorized` result instead of the intended signup return. `UpgradeWorkspace` now treats either HTTP 401 or payload `error.code=unauthorized` as auth-required and redirects to `/signup?next=/upgrade?plan=premium&billing=monthly`. Fresh Chrome on a newly started dev server at `http://127.0.0.1:3097` verified the click lands on the encoded signup URL with no Unauthorized copy and console warnings/errors `[]`; signup returned to `/upgrade?plan=premium&billing=monthly`; a second Premium monthly `Demo upgrade` activated the plan, marked Premium monthly as Current plan, showed `1,500 dreamcoins were added`, and exposed `View billing` plus `Start generating`. Disposable user `chrome-payment-proof-1783140094367@test.local` and related entitlement/subscription/checkout/ledger/session/account/preference rows were deleted, leaving 0 remaining user rows. The first focused E2E run against stale port 3000 failed at `Loading plans…`; rerunning the same test against fresh `PW_BASE_URL=http://127.0.0.1:3097` passed.

## Limits

- Real payment processing was not exercised. The validated billing path is the local mock/demo checkout; public launch still requires production BTCPay env plus a fresh live payment probe that creates a launch-test BTCPay invoice and returns a checkout URL.
- Chrome ran against local PM2 services, not deployed public HTTPS origins.
- Chrome screenshots and page reads prove visible behavior for the tested flows; automated Playwright E2E and launch probes provide broader regression coverage.
