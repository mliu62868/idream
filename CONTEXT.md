# iDream Domain Language

定义角色创作、发布运营与 Companion Chat 使用的统一语言，避免把产品事实、执行过程和展示结果混成一个概念。

## Language

**角色创作**：
围绕一个角色持续生产和维护角色内容、视觉身份及图片候选的工作。它不决定线上产品槽位，也不改变当前服务版本。
_Avoid_: 角色运营、角色发布

**角色图片库**：
归属于同一角色、可持续增加的图片集合，包含待检查、可选择和已拒绝的图片候选。图片进入图库时不绑定最终产品槽位。
_Avoid_: 三槽位资产包、场景槽位

**画面场景**：
图片内容本身表达的情境，例如卧室、约会或日常聊天。它属于图片创作 brief，不代表图片在产品中的展示位置。
_Avoid_: 产品场景、Placement

**产品槽位**：
主站消费角色图片的固定展示位置，当前为发现页封面、角色详情主视觉和聊天场景图。产品槽位只引用已经生成并通过质量检查的图片。
_Avoid_: 画面场景、生成用途

**图片候选**：
一次生成或上传得到、归属于角色图片库但尚未被产品槽位引用的图片。通过质量检查后成为可选择图片。
_Avoid_: 槽位图片、发布图片

**可选择图片**：
已经通过人物一致性和基础质量检查、可由发布运营选择到产品槽位的图片候选。通过质量检查不等于被选择或发布。
_Avoid_: 已发布图片、已采用图片

**创作交接**：
角色创作者声明一个角色内容版本、视觉身份和角色图片库已可供发布运营编排的交接动作。创作交接不创建 Release，也不改变线上角色。
_Avoid_: 提交发布、发布候选

**发布候选**：
由一个不可变角色内容版本、视觉身份和三个不同的产品槽位图片组成的完整待发布快照。批准发布候选不等于发布它。
_Avoid_: 角色创作包、已发布版本

**发布运营**：
选择产品槽位图片、检查和批准发布候选、控制发布或回滚，并记录上线后决策的工作。需要修改角色内容或生产新图片时退回角色创作。
_Avoid_: 图片创作、角色编辑

## Companion Chat

**产品 Turn**：
用户的一条消息及其唯一选中最终回复，是会话列表、历史展示、附件和计量引用的产品事实。
_Avoid_: Agent 运行、DSH SessionEvent、流式草稿

**Agent 运行**：
为一个产品 Turn 生成回复的模型、工具与流式执行过程；它可以失败或重试，但不能自行成为用户可见历史。
_Avoid_: 产品 Turn、聊天记录

**工具效果**：
Agent 工具请求产生的产品动作，例如创建图片或视频生成请求；效果身份由 Turn attempt 与 tool call 共同确定。
_Avoid_: 通用 hook、模型输出、生成结算

**陪伴记忆**：
从已提交产品 Turn 派生的长期召回内容；它可以删除、重建或暂时不可用，不是产品 Turn 权威。
_Avoid_: 产品 Turn、Agent 运行、Scene

**Main 权威快照**：
Main 为一次 Agent 运行固定的用户、角色、Soul/Release、权益和最近产品 Turn；Chat 使用但不维护这些产品事实。
_Avoid_: Chat 数据库视图、可变角色副本

**产品 Turn 修订**：
对当前会话最后一个产品 Turn 的用户消息、选中回复或存在性进行更正；修订不创建新的产品 Turn，也不允许静默改写已有后续 Turn 的因果。
_Avoid_: 历史消息覆写、会话分支

**Agent 运行接纳**：
Main 已提交产品 Turn 并固定该次执行事实，Chat 已接受以此开始或恢复对应 Agent 运行的状态；它不表示回复已经完成。
_Avoid_: 产品 Turn 提交、Agent 运行完成

**Agent 运行终态候选**：
Agent 运行提交给 Main 的不可变最终输出；只有 Main 接受后才成为产品 Turn 的选中回复。
_Avoid_: 产品回复、流式草稿

**陪伴记忆重建**：
删除产品 Turn 或 Session 后，从 Main 剩余的已提交产品 Turn 重新派生陪伴记忆，使已删除内容不再参与召回。
_Avoid_: 全量清空、历史消息恢复
