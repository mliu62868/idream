{
  "name": "@idream/chat",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local-file AgentRun runner. Main PostgreSQL owns product Turns, attachments and billing.",
  "engines": {
    "node": ">=22",
    "bun": "1.4.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "test:pure": "vitest run --config vitest.config.ts src/agent-run-store.test.ts",
    "build": "bun ../../scripts/build-bun-service.mjs src/main.ts",
    "probe:sentry": "bun src/probe-sentry.ts",
    "dev": "bun src/main.ts",
    "start": "bun dist/main.js"
  },
  "dependencies": {
    "@idream/shared": "workspace:*",
    "@sentry/node": "10.70.0",
    "dotenv": "^17.4.2",
    "ioredis": "^5.11.1",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24",
    "tsx": "^4.22.4",
    "typescript": "^5",
    "vitest": "^4.1.8"
  }
}
