# 走通角色运营、素材与发布旅程

Type: task

Blocked by: 04

## Question

在当前 Admin 真实浏览器运行态，从 CharacterWorkspace 的身份／Soul、ReferenceSetRevision、Visual route qualification、图片／视频／Voice 生成、候选审核、draft asset pack、QA、Release propose／独立批准／validate／publish 到 live Serving，运营人员是否始终看到当前状态、证据、一个可执行 next action 与精确 lineage？验证 approval 不自动采用、采用不自动发布、旧会话保持旧 pin，并记录权限、失败恢复、页面／API／console 与最终公开投影证据。
