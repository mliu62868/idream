# Today 工作台设计验收

final result: passed

日期：2026-09-06。范围：本地 Admin Today 前端优化，无数据库或领域写入契约修改。
视觉目标：聊天中第一张显示的设计稿，exec-b4fe33ba-9a51-4fe3-8eff-9f0ee2251ca2.png。
当前实现：http://127.0.0.1:3001/admin/today。

## 对照证据

源图 1488×1058；桌面以1488×1058 CSS视口采集，截图同等像素尺寸。当前首条内容举报案件选中，与源图选中对象一致；生成稿的虚构数据没有沿用。
源图与04-desktop-final.png放在同一工具输入中完成比较。已单独检查390×844窄屏。

- 03-implemented-desktop.png：第一轮实现；发现两处英文元数据标签、分类换行和列表过长。
- 04-desktop-final.png：修复后的桌面。分类缩短；中文元数据完整；事项与上下文分层；独立列表滚动。
- 05-mobile-preview.png：窄屏详情。最初自动聚焦导致返回入口被固定页头遮住，已添加滚动偏移并复验，返回入口可见，返回后焦点回到选中事项。

## 五个视觉面

- 字体：沿用后台system sans；标题20px，正文14px，辅助12px；元数据可读。
- 布局：分类、列表、详情三栏；首屏有处理入口；窄屏列表/详情切换，无横向溢出（scrollWidth=clientWidth=390）。
- 颜色：沿用--ad-*，暖灰画布、浅色面板、淡红选中态与SLA警示。
- 图片：目标没有内容图片；使用已有Lucide图标，未引入占位图或生成资产。
- 文案：真实API字段、ID、时间、验证状态；不复制设计稿虚构编号、日期、退款超时状态。

有意保留原后台shell及全部五个真实统计的紧凑入口，故顶部比源图多一条健康/新鲜度提示。源图的列表内快速排序不是现有API能力，保持服务器权威排序；全部工作的真实筛选与分页仍可用。关注、认领、置顶、推迟和批量行为保留。

## 行为验证

- 真实浏览器：选择申诉案件更新预览；去处理进入对应案件详情；全部工作第二页26–44/44；超时筛选1–15/15；移动详情返回及焦点恢复；浏览器错误日志为空。
- 定向测试36项通过：精确超时计数、分页游标、批量认领、版本冲突/超时反馈、选择预览不写入、URL分类恢复、已解决事项只读等。
- Admin TypeScript及修改文件ESLint通过；git diff --check通过。
- 本地后台Next开发进程此前Map maximum size exceeded退出，已通过项目PM2 wrapper恢复；drain/readiness/ownership/resume完成。

限制：未执行真实认领/批量写入、完整读屏审核、全仓构建或生产发布。写操作契约由现有及新增挂载测试验证。其他任务的根目录design-qa.md保持原样，本次报告独立保存在任务目录。
